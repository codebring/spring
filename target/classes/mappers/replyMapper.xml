<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.mappers.reply">

	<!-- 댓글 조회 -->
	<select id="readReply" resultType="com.mat.mvc.vo.ReplyVO">
		select
		rno, content, writer,
		regDate
		from reply
		where bno = #{bno}
	</select>

	<!-- 댓글 작성 -->
	<insert id="replyWrite" parameterType="com.mat.mvc.vo.ReplyVO">
		insert into reply(bno,
		writer, content)
		value(#{bno}, #{writer},
		#{content})
	</insert>

	<!-- 댓글 수정 -->
	<update id="replyModify" parameterType="com.mat.mvc.vo.ReplyVO">
		update reply set
		writer = #{writer},
		content = #{content}
		where rno = #{rno}
		and bno =
		#{bno}
	</update>

	<!-- 댓글 삭제 -->
	<delete id="replyDelete" parameterType="int">
		delete from reply
		where rno = #{rno}
	</delete>

	<!-- 단일 댓글 조회 -->
	<select id="replySelect"
		parameterType="com.mat.mvc.vo.ReplyVO"
		resultType="com.mat.mvc.vo.ReplyVO">
		select
		rno, bno, writer, content, regDate
		from reply
		where bno = #{bno}
		and rno = #{rno}
	</select>

</mapper>